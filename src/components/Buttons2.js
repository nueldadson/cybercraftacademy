import React from 'react';

const Buttons2 = ({text}) => {
  return <button className='w-[80px] cursor-pointer border-solid border-[1.5px] border-black bg-transparent rounded-[0.64rem] p-[0.56rem] h-full  items-center text-[0.95rem] flex justify-center text-black'>
    {text}
  </button>;
};

export default Buttons2;
